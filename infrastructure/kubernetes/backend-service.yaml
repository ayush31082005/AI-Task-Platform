apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: ai-task-platform
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - name: http
      port: 5000
      targetPort: http
