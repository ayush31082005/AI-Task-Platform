apiVersion: v1
kind: Secret
metadata:
  name: ai-task-secret
  namespace: ai-task-platform
type: Opaque
stringData:
  # Placeholder only. Replace out of band before a real deployment.
  JWT_SECRET: "replace-with-a-secure-secret"
